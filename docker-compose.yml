version: '3.9'

services:
  api:
    build: .
    container_name: api_emprestimo
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      - db

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "8501:8501"
    depends_on:
      - api
    volumes:
      - .:/app

  db:
    image: postgres:15
    container_name: postgres_emprestimo
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: riscos
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  seed:
    build: .
    command: python app/seed.py
    depends_on:
      - db
    env_file:
      - .env

volumes:
  pgdata: